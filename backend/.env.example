# The .env.example file is a template with placeholder values

# Copy this file to .env and fill in your values
DATABASE_URL=postgresql://preppy_user:yourpassword@localhost:5432/preppy_dev

# OR use component variables:
DB_USER=preppy_user
DB_PASSWORD=yourpassword
DB_HOST=localhost
DB_PORT=5432
DB_NAME=preppy_dev